


apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: a0528075188-dev
  name: 'server'
  annotations: {}
spec:
  selector:
    matchLabels:
      app: name
  replicas: 1
  template:
    metadata:
      labels:
        app: name
    spec:
      containers:
        - name: server-container
          image: >-
            shmuelblau/test:laster
          ports:
            - containerPort: 8000
              protocol: TCP
          env: 
           - name: HOST
             value: 
           - name: DATABASE
             value: "test"
           - name: USER
             value: "shmuel"
           - name: PASSWORD
             value: "1234"
           - name: TABLE_NAME
             value: "table_test"

      imagePullSecrets: []
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  paused: false
